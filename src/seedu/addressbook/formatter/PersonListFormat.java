package seedu.addressbook.formatter;

/** Enum to indicate how a given List should be formatted*/
public enum PersonListFormat {
    ALL_PUBLIC_DETAILS,
    ACCOUNT_DETAILS,
    NAMES_ONLY,
    FEES_DETAILS,
    FEES_DUE_DETAILS
}
