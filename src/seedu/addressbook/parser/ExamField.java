package seedu.addressbook.parser;

/**
 * Enum used to indicate which exam field a string belongs to
 * */
public enum ExamField {
    examName,
    subjectName,
    examDate,
    examStartTime,
    examEndTime,
    examDetails,
    isPrivate
}
